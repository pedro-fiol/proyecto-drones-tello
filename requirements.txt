# Tello Interceptor — runtime dependencies
# Python 3.12 recommended. PyTorch CUDA 12.1 wheels.
#
# Install:
#   pip install -r requirements.txt
#
# CPU-only fallback: replace torch/torchvision lines with
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# --- Drone SDK ---
djitellopy==2.5.0

# --- Computer vision ---
opencv-python==4.13.0.92
numpy==2.4.3
av==17.0.1                          # PyAV — H.264 frame decode

# --- Deep learning (CUDA 12.1) ---
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# --- Perception models ---
ultralytics==8.4.41                 # YOLOv8s-pose
torchreid==0.2.5                    # OSNet ReID embedder

# --- Web dashboard (FastAPI + WebSocket + MJPEG) ---
fastapi==0.136.1
uvicorn[standard]==0.47.0           # pulls websockets, httptools, watchfiles
